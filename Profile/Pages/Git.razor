@page "/Git"
@inject NavigationManager NavigationManager

<Redirect PageName="GitHub"></Redirect>

@code {
    protected override async Task OnInitializedAsync()
    {
        await Task.Delay(3000);
        NavigationManager.NavigateTo("https://github.com/AriDevK");
    }
}