@page "/Medium"
@inject NavigationManager NavigationManager

<HeadTitle Title="Redirect"></HeadTitle>
<Redirect PageName="Medium blog"></Redirect>

@code {
    protected override async Task OnInitializedAsync()
    {
        await Task.Delay(3000);
        NavigationManager.NavigateTo("https://medium.com/@AriDev");
    }
}